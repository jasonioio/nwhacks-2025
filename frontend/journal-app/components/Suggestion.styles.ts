import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  suggestionText: {
    margin: 20,
    fontSize: 16,
    fontStyle: "italic",
    color: "gray",
    textAlign: "center",
  },
});
